name: Release Application to ghcr.io and Artifacts

on:
  workflow_dispatch:

permissions:
  contents: write
  packages: write
  id-token: write

jobs:
  release:
    uses: umatare5/common/.github/workflows/goreleaser.yml@eb4ce45eb7d7e2b7a88454e12786e6ab29463ab7
    with:
      go_version: "1.24.5"
      goreleaser_version: "v2.11.1"
